# GEM 시스템 작업 내역 복구 가이드
생성일: 2026-01-08

## 현재 상태
- **HEAD**: 341155f (2025-12-29) - 프롬프트 UX 개선
- **롤백 전**: aa7135d (2026-01-06) - 설정 관리 시스템 및 스케줄러 구축 완료
- **상태**: 12월 29일 버전으로 완전 롤백 완료

---

## 1월 6일 작업 내역 (aa7135d 커밋)

### 주요 변경사항

#### 1. **스케줄러 시스템 구축** ⚠️ (문제 원인)
- **파일**: `modules/scheduler/data_scheduler.py` (신규 489줄)
- **기능**:
  - 00:30 일일 CSV 검증 (중복 제거, 갭 채우기)
  - 09:00 일일 분석 (Phase 1→2→3 자동 실행)
  - APScheduler 사용
- **문제**: FastAPI deprecation warning, 여러 의존성 문제
- **결론**: 당장 불필요, 복구 제외

#### 2. **하드코딩 제거** ✅ (복구 필요)
**a) 채널 목록**
- **변경 전**: `realtime.py`에 하드코딩 `["BSC_CH_01" ~ "BSC_CH_06"]`
- **변경 후**: `central_config.yaml`에서 읽기
- **파일**:
  - `config/central_config.yaml` (+8줄)
  - `api_collector/realtime.py` (54행)
  - `api_collector/historical.py` (47행)

**b) 집계 간격**
- **변경**: `api_collector/config.py`에 "1s" 옵션 추가
- **이유**: 1초 간격 수집 요구사항

**c) 센서 임계값**
- **파일**: `config/battery_profiles/*.yaml` (4개 파일 수정)
- **변경**: LFP: 0.0005, NMC: 0.0015 임계값 하드코딩 제거

#### 3. **API 호출 로그 개선** ✅ (복구 필요)
- **파일**: `api_collector/realtime.py`, `historical.py`
- **변경**:
  - API 호출 정보 터미널 출력 추가 (`print()` 추가)
  - 저장 완료 로그 터미널 출력 추가
  - 로그 레벨 DEBUG → INFO 변경 (일부)
- **효과**: 사용자가 API 호출 상태를 실시간으로 볼 수 있음
- **중요도**: ⭐⭐⭐⭐⭐ (이게 없어서 오늘 문제 발생)

#### 4. **CSV 검증 강화**
- **파일**: `modules/validators/csv_validator.py` (신규 464줄)
- **기능**:
  - 중복 제거
  - 갭 채우기
  - 정렬
  - 진행 상황 표시

#### 5. **log_callback 추가**
- **파일**: `main.py`, `realtime.py`, `historical.py`, `storage.py`
- **기능**: WebSocket으로 로그 실시간 브로드캐스트
- **변경**:
  - `broadcast_log()` 함수 추가
  - 모든 collector에 `log_callback` 파라미터 추가
  - `DataStorage(log_callback=broadcast_log)` 초기화

#### 6. **프론트엔드 개선**
- **파일**: `static/app.js`, `index.html`, `style.css`
- **변경**:
  - 수집 상태 UI 개선
  - 채널별 진행 상황 표시
  - 전체 진행률 표시

#### 7. **데이터 소스 관리**
- **파일**: `modules/prompt/api_data_commands.py` (+298줄 변경)
- **기능**: 3가지 소스 자동 스캔 (realtime/batch/uploads)

#### 8. **불필요한 파일 제거**
- `restart_ollama.bat`, `start_ollama.bat` 삭제
- `start_web_ui.bat`, `start_web_ui.sh` 삭제
- `config/battery_profiles/profiles/` 폴더 삭제 (통합)

---

## 복구 우선순위

### 🔴 긴급 (즉시 복구 필요)
1. **API 호출 로그 터미널 출력**
   - 파일: `realtime.py`, `historical.py`
   - 이유: 사용자가 수집 상태를 볼 수 없음

2. **채널 목록 central_config.yaml에서 읽기**
   - 파일: `central_config.yaml`, `realtime.py`, `historical.py`
   - 이유: 유연한 채널 관리

### 🟡 중요 (필요시 복구)
3. **log_callback 시스템**
   - 파일: `main.py`, collectors
   - 이유: WebSocket 실시간 로그

4. **CSV 검증 모듈**
   - 파일: `modules/validators/csv_validator.py`
   - 이유: 데이터 품질 관리

5. **프론트엔드 개선**
   - 파일: `app.js`, `index.html`, `style.css`
   - 이유: 사용자 경험

### ⚪ 선택 (나중에)
6. **1초 집계 간격 옵션**
   - 파일: `api_collector/config.py`

7. **센서 임계값 YAML 수정**
   - 파일: `battery_profiles/*.yaml`

### ❌ 제외 (복구 안 함)
8. **스케줄러 시스템** - 문제 원인, 당장 불필요

---

## 단계별 복구 방법

### Step 1: API 호출 로그 복구
```bash
git show aa7135d:GEM/api_collector/realtime.py > temp_realtime.py
# 200-210행, 330-360행 로그 부분만 수동 복사
```

### Step 2: 채널 목록 설정 복구
```bash
git show aa7135d:GEM/config/central_config.yaml > GEM/config/central_config.yaml
# realtime.py 54행 수정: self.channels = central_config.get_channels()
```

### Step 3: log_callback 복구
```bash
# main.py에 broadcast_log 함수 추가
# collectors 초기화 시 log_callback 파라미터 추가
```

### Step 4: CSV 검증 모듈 복구
```bash
git show aa7135d:GEM/modules/validators/csv_validator.py > GEM/modules/validators/csv_validator.py
git show aa7135d:GEM/modules/validators/__init__.py > GEM/modules/validators/__init__.py
```

---

## 파일별 변경 통계

| 파일 | 추가 | 삭제 | 비고 |
|------|------|------|------|
| main.py | 310줄 | - | log_callback, 스케줄러 |
| realtime.py | 171줄 | - | log_callback, 터미널 로그 |
| historical.py | 72줄 | - | 동일 |
| storage.py | 245줄 | - | log_callback |
| data_scheduler.py | 489줄 | - | 신규 (제외) |
| csv_validator.py | 464줄 | - | 신규 |
| app.js | 138줄 | - | UI 개선 |

**총 변경**: 58개 파일, +4,910줄, -17,530줄

---

## 백업 파일
- `_dev/ROLLBACK_BACKUP_SCHEDULER_20260108.txt` - aa7135d 커밋 메시지
- `_dev/작업내역_복구가이드_20260108.md` - 본 문서

---

## 참고사항

### 현재 정상 작동하는 버전
- **341155f (2025-12-29)**: 프롬프트 UX 개선
- **문제**: API 호출 로그가 DEBUG 레벨이라 터미널에 안 보임

### aa7135d 버전 문제점
1. 스케줄러 import 에러
2. FastAPI deprecation warning
3. get_channels() 메서드 위치 불일치
4. 과도한 리팩토링으로 디버깅 어려움

### 교훈
- ⚠️ 대규모 리팩토링은 단계별로 커밋
- ⚠️ 각 커밋마다 서버 시작 테스트 필수
- ⚠️ 스케줄러 같은 큰 기능은 별도 브랜치에서 작업
