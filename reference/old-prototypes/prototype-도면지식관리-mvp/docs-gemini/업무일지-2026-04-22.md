# 업무일지 2026-04-22

## 오늘 한 일

### 1. Codex 실행 경고 확인

- `bubblewrap` 경고의 의미 확인
- 시스템 PATH에 `bubblewrap`이 없지만 Codex 내장 버전을 대신 사용한다는 경고로 정리
- 당장 작업을 막는 오류는 아니며, 필요 시 `sudo apt install bubblewrap`으로 해소 가능하다고 판단

### 2. 이전 세션 흔적 확인

- 현재 프로젝트 경로가 git 저장소로 인식되지 않는 상태 확인
- 최근 수정 파일 기준으로 이전 작업 흔적 확인
- `docs/ai-3d-builder/SESSION-HANDOFF-2026-04-22.md` 발견
- 해당 파일에 이전 3D Vision MVP 관련 인수인계 내용이 남아 있음을 확인
- 단, 이번 작업과는 별도 트랙으로 보고 현재 논의에서는 제외

### 3. SaaS UX / Workflow 전략 문서 검토

`docs-gemini` 안의 아래 문서들을 읽고 현재 방향을 재정리함.

- `xd_ai_decision_framework.md`
- `xd_ai_destination_services.md`
- `xd_ai_ux_delivery_strategy.md`
- `xd_ai_workflow_strategy_roadmap.md`
- `saas_ux_advancement_strategy.md.resolved`

검토 결론:

- 단순 AI 채팅이나 예쁜 UX보다, 사용자의 실제 업무를 끝내주는 워크플로우가 핵심
- v0, Perplexity, Manus, Genspark의 공통점은 텍스트 답변이 아니라 산출물과 작업 대체
- 초기 SaaS 기능은 여러 기능을 동시에 만들기보다 하나의 워크플로우를 강하게 검증해야 함

### 4. Workflow Lab 목업 1차 구현

- 기존 앱 구조 확인
- `/design-lab`, `/insight`, `/concept-lab` 구조 확인
- `src/app/workflow-lab/layout.tsx`는 이미 있었지만 `page.tsx`가 없음을 확인
- 신규 목업 주소를 `/workflow-lab`로 결정

1차 구현 내용:

- `src/app/workflow-lab/page.tsx` 생성
- `PUMP-01 위치와 관련 자료 찾아줘` 시나리오 기반 목업 구현
- 좌측 도면 캔버스
- 중앙 워크플로우 실행 과정
- 우측 evidence / citation 패널
- 하단 SaaS 판단 지표 구성

추가 문서:

- `docs-gemini/xd_ai_p0_workflow_lab_mockup_plan.md`

### 5. 실제 온톨로지 분석 데이터 확인

사용자 지시에 따라 아래 실제 분석 결과 폴더 확인.

- `dwg/온톨로지`

확인한 구조:

- 분야별 분석 결과 폴더 존재
- `entity_list_data.yaml`
- `entity_list_page_*.yaml`
- `entity_details/*.yaml`
- `ontology_placement/*.yaml`
- `ontology_rules/*.yaml`

중요 판단:

- 기존 `PUMP-01` 목업은 이해용 가상 시나리오였고 실제 데이터와 직접 맞지 않음
- 실제 데이터 기준으로는 `SMFD-01`이 좋은 검수 시나리오임
- `SMFD-01`은 page 50, page 53에 등장
- `SMFD-01 -> SMFD-02`, `SMFD-01 -> JB-A-01`, `SMFD-01 -> LEGEND-CABLE`, `PANEL-ROOF-PLAN -> SMFD-01` 등의 실제 관계가 있음
- `placement_SMFD-01.yaml`에 OWL 변환 후보가 존재

### 6. Workflow Lab 목업 2차 재구성

실제 데이터 기반으로 `/workflow-lab`을 재구성.

핵심 시나리오:

```text
SMFD-01을 OWL로 넣어도 되는지 근거를 따라 검수해줘
```

구성:

- 실제 분석 후보 엔티티 목록
- page 50 / page 53 근거 표시
- `SMFD-01` 상세 스펙 표시
- relation 후보 표시
- OWL placement 후보 표시
- 조건부 승인 / 보류 리스크 표시

제품 방향 재정의:

- 단순 설비 위치 검색 SaaS가 아니라
- AI 도면 분석 결과를 OWL 변환 전에 검수하는 `Ontology Evidence Review Workflow`가 현재 데이터 상태에 더 적합

### 7. Workflow Lab 메뉴 누적 구조로 수정

사용자 피드백:

- 기존 목업을 지우지 말고 메뉴로 계속 추가하는 방식이 좋겠다는 의견

반영:

- `/workflow-lab` 상단에 `Workflow Lab Menu` 추가
- 목업을 지우지 않고 누적 비교하는 구조로 변경

현재 메뉴:

- `실제 SMFD 검수`
  - 실제 `dwg/온톨로지` YAML 기반
  - 기본 표시 화면
- `초기 네비게이터 컨셉`
  - 기존 `PUMP-01` 가상 SaaS 흐름 목업 복구
  - 비교용 컨셉으로 유지

### 8. 서버 및 검증

- Next dev 서버 실행
- `/workflow-lab` 응답 `200 OK` 확인
- dev 서버가 오래된 번들을 계속 서빙하는 문제 발견
- 기존 Next dev 서버 프로세스 종료 후 재시작
- 최신 HTML에 `Workflow Lab Menu`, `실제 SMFD 검수`, `초기 네비게이터 컨셉` 문구가 포함되는 것 확인
- `src/app/workflow-lab/page.tsx` 단독 ESLint 통과 확인

### 9. 세션 종료 전 정리

- 실행 중이던 Next dev 서버 프로세스 종료
- `ps` 기준 `next dev` 프로세스가 남아 있지 않음을 확인
- G 드라이브 경로는 현재 WSL 세션에서 `/mnt/g`로 접근되지 않음
- 그래서 프로젝트 내 `docs-gemini/업무일지-2026-04-22.md`에 오늘 작업 내용을 남김

## 남은 이슈

- `G:\내 드라이브\_Obsidian\지식관리\업무일지` 경로는 현재 WSL 세션에서 접근 불가
- 추후 G 드라이브가 WSL에 마운트되면 이 내용을 Obsidian 업무일지로 옮기면 됨
- 전체 `npm run build`는 기존 코드 타입 오류 때문에 실패
  - `src/lib/insight/llm-client.ts`
- 전체 `npm test`는 현재 Node `v18.19.1`과 Vitest/Vite/Rolldown 요구 버전 불일치로 실패
  - Node 20 이상 필요

## 다음 작업 제안

1. `/workflow-lab`에 목업 3 추가
   - `SMFD-01` 단일 검수에서 `SMFD 제어 구역 / 그룹 관계 검수`로 확장
2. 실제 YAML을 직접 읽는 mock data loader 구성
   - 현재는 실제 데이터를 보고 수동 반영한 상태
   - 다음 단계에서는 YAML 일부를 파싱해 화면에 연결
3. Review SaaS UX 강화
   - 승인 / 보류 / 병합 후보 / unknown 큐를 실제 검수 플로우처럼 설계

