reason: 독립 tag이며 SCHEDULE 타입으로 자체 사양(specs.labels_inside/raw_labels/columns/rows_count/schedule_items)과
  관계(relations 2개), 위치(position), 출처(source_pages/source_attribution/collection_sources),
  도면 텍스트가 모두 확인됨. page 23 context에서 동일 표의 관계와 연결 대상(BLK-SS-RCENTER)도 확인되며, 도면 메타데이터가
  아닌 물리 설비가 아니므로 정보는 schedule individual로 보존하되 독립 엔티티로 qualified 처리.
tag: TBL-RIGHT
verdict: qualified
